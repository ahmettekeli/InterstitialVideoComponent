import styled from "styled-components";

export const Wrapper = styled.div`
position:relative;
video{
    width: 100%;
    height: auto;
}`;

export const Controls = styled.div`
 position: absolute;
 top: 0;
 left: 0;
 padding: 0.5rem;
 color: white;
 z-index: 1;`;